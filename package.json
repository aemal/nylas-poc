{
  "name": "nylas",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch index.ts",
    "pubsub:setup": "bun run setup-pubsub.ts",
    "pubsub:ngrok": "bun run setup-ngrok.ts",
    "pubsub:pull": "bun run pull-messages.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "axios": "^1.8.4",
    "fastify": "^5.3.0",
    "nylas": "^7.8.0"
  }
}
